import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  icon: {
    color: 'white',
    fontSize: 100,
  },
  title: {
    fontSize: 30,
  },
  subTitle: {
    fontSize: 20,
  },
});

export default styles;
